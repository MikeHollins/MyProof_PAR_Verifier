# Final completion matrix

Status: `PARTIAL` — checkpoint-1 local candidate evidence only. This is the
single status roll-up for the standalone candidate; it is not a release sign-
off and does not replace the immutable execution contract.

Authority: immutable contract revision
`3afbc6fc1a4347a7a583347e70630ccd96c8ddb0`,
`docs/plans/PAR_VERIFIER_CLI_MCP_EXECUTION_CONTRACT.md` in the AgeVerify
repository. This matrix was assembled on 2026-08-31 from the current
worktree records linked below. Those records are not final evidence until the
root integrator commits the candidate and records the resulting revisions.

Status values are deliberately scoped: `PROVEN` means directly exercised at
the stated local scope; `PARTIAL` means local evidence exists but an explicit
integration, remote, deployment, or release part remains; `MISSING` means no
authoritative evidence is recorded; `CONTRADICTED` means current evidence
conflicts with the gate. No current gate is classified `CONTRADICTED`.

Evidence records:

- [contracts/provider evidence](CHECKPOINT_1_CONTRACTS_PROVIDER_EVIDENCE.md)
- [core/cryptography evidence](CHECKPOINT_1_CORE_CRYPTO_EVIDENCE.md)
- [service-facade evidence](CHECKPOINT_1_SERVICE_EVIDENCE.md)
- [CLI/package/CI evidence](CHECKPOINT_1_CLI_PACKAGE_CI_EVIDENCE.md)
- [MCP evidence](CHECKPOINT_1_MCP_EVIDENCE.md)
- [foundation contract](FOUNDATION_CONTRACT.md)

## Product, foundation, and production gates

Repeated requirements are cross-referenced here rather than restated in each
later section. The `Next authoritative action` column is a required action,
not evidence that it has already run.

| Contract gate                                                                                                                                                                          | Current authoritative evidence                                                                                                                                                                                                   | Status           | Next authoritative action                                                                                                                                                                                                                       |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| §2–§3; §7 increment 1: immutable authority, repository identities, frozen bases, standalone initial commit, remote, and rollback point                                                 | Every checkpoint record names the immutable contract revision. The standalone staging tree currently has no `HEAD` or configured remote, and no durable baseline/rollback record is present.                                     | `MISSING`        | After integration, record `git rev-parse HEAD`, `git remote -v`, clean status, each changed repository's exact commit, and rollback refs before any remote or deployment action.                                                                |
| §5.1; §7 increment 2; §14: exact package, CLI, MCP, resource, prompt, and export surface                                                                                               | `README.md`, `FOUNDATION_CONTRACT.md`, `test/contracts/public-surface.test.ts`, `test/cli/parse.test.ts`, `test/mcp/adapter.test.ts`; clean-copy package smoke reports 144 packed files and the exact public import/bin surface. | `PROVEN` (local) | Re-run `npm run test:unit -- --reporter=dot` and package smoke from the committed clean checkout; preserve the package manifest and export-map result in the release record.                                                                    |
| §5.2; §7 increment 2: canonical report identifier, finite fields, check tuple/order/coherence/active invariants, and shared Zod/JSON Schema semantics                                  | Contracts/provider evidence records strict contract tests, generated artifacts, Ajv/Zod adversarial parity, and object-root `$ref`/`$defs` checks; core and MCP records parse reports through the shared schema.                 | `PROVEN` (local) | On the integration commit run `npm run schema:check`, `npm exec -- vitest run test/contracts test/provider/http.test.ts`, and raw MCP `tools/list` schema comparison without normalization.                                                     |
| §5.3; §7 increment 5: CLI parser, human/JSON output, and exit codes `0/10/20/21/64/70`                                                                                                 | `test/cli/classify.test.ts` maps the domain outcomes; `test/cli/run.test.ts` covers human output, JSON, usage, invariant, cancellation, and stream discipline; MCP parity covers shared reports and domain codes.                | `PARTIAL`        | From the committed package, run and retain redacted process-level CLI golden cases for coherent active/inactive, contradictory, indeterminate, usage, internal-invariant, and cancellation paths, including exact stdout/stderr and exit codes. |
| §5.4; §12; §14: truthful claim boundary and no unsupported proof, acceptance, identity, epoch, transparency, or quantum-security claim                                                 | `README.md`, `FOUNDATION_CONTRACT.md`, core evidence, and MCP parity explicitly preserve `NOT_PERFORMED` and `PAR_REPORTED_ONLY`; adversarial tests reject remote prose and injected fields.                                     | `PROVEN` (local) | Root and independent auditor must perform a final claims scan against the committed report and public docs before release; any overclaim is a stop condition.                                                                                   |
| §6: caller accepts only canonical asset ID plus `require_active`; no caller origin, URL, credential, key, proof, or raw evidence                                                       | Contract/provider, CLI parser, MCP adapter, and provider tests reject unknown fields, unsafe UUID/path values, and caller-selected transport inputs.                                                                             | `PROVEN` (local) | Re-run the strict input and unknown-field corpus in the committed clean checkout, then retain the package and MCP raw-input results.                                                                                                            |
| §6: fixed `https://par.myproof.ai` origin, finite canonical paths, GET-only transport, redirect rejection, timeout/size/content-type/abort/concurrency limits, and fail-closed parsing | `test/provider/http.test.ts` covers deterministic URL/path, redirect, byte, timeout, abort, concurrency, content-type, and mutation controls; the service record has a bounded canonical JWKS/status observation.                | `PARTIAL`        | After PAR/website integration, record immutable deployment revisions and run the provider's bounded canonical asset verification plus DNS/TLS/live compatibility probe; stop on any origin or route ambiguity.                                  |
| §6: authenticated release-manifest/live-JWKS intersection, signed ES256 status, explicit receipt/status/bundle/asset/policy bindings, and public-commitment limitation                 | Core/cryptography evidence records fixed-vector signatures, manifest pinning, live-key intersection, status checks, binding mutations, rotation simulation, and the explicit no-preimage limitation.                             | `PARTIAL`        | Land and record PAR historical public-key publication and live-key evidence, then run the committed verifier against the deployed ring; do not treat embedded fixture keys or structural status values as trust roots.                          |
| §6: no database or production credentials; no unsafe fresh-verification route dependency                                                                                               | Standalone provider source/tests use bounded read-only fetches and redaction; package security evidence has zero secret findings; docs keep the unsafe route outside the surface.                                                | `PARTIAL`        | PAR/website owners must provide a read-only route/dependency audit and a negative runtime/query check proving the standalone verifier never reaches the unsafe route or production database.                                                    |
| §7 increment 3: pure deterministic verification core                                                                                                                                   | Core/cryptography record covers immutable signed bytes, real ES256 mutations, status/receipt bindings, trust, rotation, and report parsing; pure-core tests perform no network I/O.                                              | `PROVEN` (local) | Re-run the immutable fixture and focused core/crypto suite from the committed tree, then attach the exact seven-hash and result record to the integration revision.                                                                             |
| §7 increment 4: bounded live provider                                                                                                                                                  | Contracts/provider and service records cover provider schema, producer fixture identity, transport defenses, and redacted failures; canonical live observation is explicitly WIP and not asset verification.                     | `PARTIAL`        | After production prerequisites, run the bounded provider against a non-sensitive canonical test asset and record only redacted status, hashes, counts, and request-boundary evidence.                                                           |
| §7 increment 6: one stdio MCP adapter using the shared facade and schemas                                                                                                              | MCP evidence covers one tool, zero resources/prompts, raw schema equality, shared cancellation, protocol errors, bounded duplicated output, and no legacy `{result: report}` wrapper.                                            | `PROVEN` (local) | Re-run the packaged stdio lifecycle from an empty consumer and raw `tools/list` after the integration commit; retain stdout-only JSON-RPC and clean EOF evidence.                                                                               |
| §7 increment 7; §8 durable release prerequisite 1: PAR historical public key ring, protected `kid`, full receipt-validity/rollback horizon                                             | Local core/service tests simulate overlapping and retired keys, but every evidence record explicitly says deployed PAR retention/publication is unproven.                                                                        | `MISSING`        | In the PAR repository land the additive historical ring, record commit/deployment refs, run old/current/unknown-kid and post-rollback receipt probes, and preserve the retention-horizon evidence.                                              |
| §7 increment 7; §8 durable release prerequisite 2: additive `GET .../verification-bundle?audit=omit` with runtime/query proof of no audit aggregation                                  | The exact producer fixture and standalone consumer schema accept only the bounded omitted-audit placeholders; no PAR runtime/query non-invocation record is present.                                                             | `MISSING`        | In PAR, run the route and query instrumentation against the exact producer fixture, prove audit aggregation is not invoked, record route/commit evidence, and compare the emitted public wire byte-for-byte.                                    |
| §7 increment 8; §8 durable release prerequisite 3: website canonical `REGISTRY_URL`, no silent fallback, exact deployment revision                                                     | The standalone live probe reaches canonical origin, but service/MCP records explicitly do not certify website configuration or deployment state.                                                                                 | `MISSING`        | Set and verify `REGISTRY_URL=https://par.myproof.ai` in the production website environment, deploy the exact reviewed commit, record the immutable deployment ID, and run the changed-path live probe.                                          |
| §7 increment 9; §9 cross-repository compatibility/release                                                                                                                              | PAR fixture bytes and standalone copy match locally; package, workflow, security, and origin evidence are local clean-copy records, while PAR/website deployment and remote enforcement remain open.                             | `PARTIAL`        | After all repository commits exist, run the cross-repository fixture/hash/consumer matrix from clean checkouts, record every commit and deployment, and classify each gate as enforced, advisory, unavailable, or not evaluated.                |

## Release tiers and stop conditions (§8, §15, §16)

| Contract gate                                                                                                                                                                                                                | Current authoritative evidence                                                                                                                                                                             | Status    | Next authoritative action                                                                                                                                                   |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| §8 deterministic fixture release: may ship only as a truthful non-live fixture release                                                                                                                                       | Local foundation, contract, core, provider, CLI, MCP, package, and security checks are strong, but the standalone candidate has no integration commit, provenance record, or release artifact publication. | `PARTIAL` | Commit the candidate, rerun clean-checkout package/provenance checks, and publish only if the resulting release text preserves the non-live claim boundary.                 |
| §8 bounded private beta: explicit origin, deployment, and key freeze; stop on unknown/rotated key                                                                                                                            | A WIP canonical JWKS/status observation exists, but no deployment revision or explicit key freeze is recorded.                                                                                             | `MISSING` | Record the approved origin, immutable deployment, and frozen live key set before any private-beta probe; fail closed on rotation or unknown `kid`.                          |
| §8 durable public release: all three PAR/website prerequisites and every §9 gate                                                                                                                                             | Historical-key retention, `audit=omit` runtime/query evidence, website canonical configuration, remote CI enforcement, rollback, and independent audit remain open in the rows above.                      | `MISSING` | Close every prerequisite and §9 row, then have the root integrator and independent auditor confirm no hard gate remains open.                                               |
| §15 stop conditions: ambiguous origin/revision, incomplete key horizon, audit invocation, unsafe route, caller-controlled transport, CLI/MCP divergence, failed protocol/package/privacy/CI/deploy/provenance/overclaim gate | No current contradictory evidence is recorded, but multiple stop conditions are unmet because their required evidence is missing; the candidate must not be declared complete.                             | `PARTIAL` | Preserve the affected lane and evidence, stop release activity, and resume only after the exact missing gate and its independent verification are recorded.                 |
| §16 final completion statement                                                                                                                                                                                               | Current local candidate evidence does not include integrated revisions, remote CI health/enforcement, production prerequisites, live asset verification, rollback, or the independent Sol score.           | `PARTIAL` | Root may mark complete only after all required rows are `PROVEN`, the final packet is committed, and the independent score is at least 97/100 with no blocker/high finding. |

## Compatibility, CI, and release gates (§9)

| Gate                                                                                                                               | Current authoritative evidence                                                                                                                                                                                                           | Status           | Next authoritative action                                                                                                                                                                                                           |
| ---------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Format, lint, strict typecheck, build, unit, integration, and deterministic fixture tests                                          | Clean-copy record: `npm run schema:check`, format, lint, check, build, `test:unit` (`17 files / 291 tests`), and `test:all` (`20 files / 296 tests`) passed; immutable vector is 1 file/2 tests with seven hashes.                       | `PROVEN` (local) | Run the same commands from the committed checkout and capture remote check names/results; do not infer remote health from this local pass.                                                                                          |
| Supported Node matrix and Linux/macOS coverage; truthful Windows treatment                                                         | Workflow-contract evidence verifies Node `22.19.0`/`24.0.0` on Ubuntu `24.04`/macOS `14`; Windows is explicitly documented as not evaluated because the stdio harness is POSIX-targeted.                                                 | `PARTIAL`        | Trigger the committed workflow and retain all four quality-cell results; preserve the explicit Windows exclusion or add a supported Windows cell with its own protocol evidence.                                                    |
| CLI/MCP byte and semantic parity over every golden/domain outcome                                                                  | MCP parity locally compares shared reports, CLI JSON, MCP structured content/text, reason codes, domain codes, cancellation, and the exact producer omit artifact; packaged CLI golden output is not durably recorded.                   | `PARTIAL`        | Run packaged CLI and MCP cases from one clean install, compare canonical bytes and exit/error behavior for every fixture, and attach the redacted parity table.                                                                     |
| Cryptographic mutations, algorithm confusion, malformed tokens, wrong issuer/audience/subject/kid, expiry and clock boundaries     | Core/cryptography and provider records enumerate real ES256/JWS/status/trust/mutation suites and immutable bytes.                                                                                                                        | `PROVEN` (local) | Re-run the focused crypto/provider commands on the integration commit and include their exact result counts in the independent review packet.                                                                                       |
| Signed, unsigned, malformed, stale, revoked, suspended, unknown, and unreachable status cases                                      | Core/service/provider records cover signed status, suspension, malformed/unknown/tampered/expired credentials, unavailable transport, and active-condition classification; unsigned JSON is rejected by the contract.                    | `PROVEN` (local) | Preserve the status matrix in the committed evidence and rerun it after any PAR status-route change.                                                                                                                                |
| Key rotation, overlap/retirement, unknown keys, manifest tampering, and rollback behavior                                          | Local tests cover overlap, retired/unknown keys, digest pinning, and manifest mutations; production key history and rollback rehearsal are absent.                                                                                       | `PARTIAL`        | Complete the PAR retention and rollback probes in the production prerequisite rows, then rerun old/new receipts through the deployed verifier before release.                                                                       |
| SSRF/path injection, redirects, DNS/origin invariants, timeout, byte, concurrency, abort, decompression, and content type          | Provider mutation matrix and focused HTTP tests cover local deterministic defenses; fixed-host DNS/TLS evidence exists, but deployed asset-path live behavior is not proven.                                                             | `PARTIAL`        | Run the bounded canonical live provider probe against the immutable website/PAR revisions and retain redacted request-boundary results.                                                                                             |
| Privacy and prompt-injection resistance; no raw evidence, credentials, identifiers, tokens, or secrets in reports/diagnostics      | Contract/core/service/CLI/MCP tests reject injected fields and redact provider/transport text; clean-copy security evidence reports zero secret findings.                                                                                | `PROVEN` (local) | Repeat the redaction/adversarial corpus after integration and have the independent auditor inspect all public output and evidence artifacts for leakage.                                                                            |
| Real stdio MCP lifecycle and Inspector checks                                                                                      | MCP record reports packaged lifecycle, cancellation, clean EOF, legacy `tools/list`, and modern Inspector `server/discover`; clean-copy hooks passed stdio and Inspector.                                                                | `PROVEN` (local) | Rerun the pinned Inspector and packaged lifecycle from the committed empty consumer and record exact protocol/version outputs.                                                                                                      |
| Applicable official MCP conformance through test-only loopback while product remains stdio-only                                    | MCP record reports pinned conformance `0.1.16`, spec `2025-11-25`, applicable `server-initialize`, `ping`, and `tools-list` all passed; intentional exclusions are documented.                                                           | `PROVEN` (local) | Execute the same single conformance invocation in remote CI and retain the scenario catalog plus exclusion rationale.                                                                                                               |
| `npm pack`, package-content inspection, empty-consumer install/execution, provenance, and checksum verification                    | Clean-copy package-smoke passed: 144 files, 125,355-byte tarball, SHA-256 `b6756af9845676eb9758b31a7c1992deb0be07eba8ae40483407251519f2b3ec`; provenance intentionally failed in the source-only copy because `.git`/`HEAD` was omitted. | `PARTIAL`        | After the integration commit, rerun package smoke and provenance from a clean Git checkout with `CI=true GITHUB_SHA=<integration-commit>`; record matching commit, package digest, and empty-consumer output.                       |
| SBOM, license/dependency review, secret scan, npm audit, and build provenance                                                      | Clean-copy security record reports 387 lockfile packages/licenses, 16 direct dependencies, zero secret findings, clear npm audit, CycloneDX SBOM 1.5, and lockfile hash; durable build provenance is not yet recorded.                   | `PARTIAL`        | Run security/provenance scripts from the committed checkout, persist redacted outputs with the exact commit/package digest, and classify dependency-review availability.                                                            |
| Linux/macOS CI, Windows support decision, trigger coverage, enforcement truth, and changed-path/self-trigger mutation coverage     | Workflow-contract script proves configured pull request, push-to-main, merge queue, manual dispatch, concurrency, pins, permissions, no path filters, and invocation selection; no remote run or mutation result is recorded.            | `PARTIAL`        | Trigger PR, push, merge-queue, and manual-dispatch runs for the committed workflow, record check IDs/enforcement, and run changed-path/self-trigger mutations; preserve the documented Windows `not evaluated` status if unchanged. |
| PAR/website baseline suites, supported-client fixtures, public/candidate wire compatibility, and exact cross-repository comparison | Standalone producer fixture is byte-identical to the PAR artifact and provider tests pass; PAR/website baseline and deployed supported-client evidence are not in this worktree.                                                         | `PARTIAL`        | Run each changed repository's baseline and supported-client suites from the exact recorded commits, compare raw and canonical wire bytes, and attach both repository/deployment revisions.                                          |
| Immutable deployment binding, canonical origin, and changed-path live probe                                                        | Origin evidence proves bounded DNS/TLS for `par.myproof.ai`; no website/PAR immutable deployment ID or asset-specific live verification is recorded.                                                                                     | `MISSING`        | Deploy exact reviewed commits through approved production paths, record deployment IDs and canonical configuration, then run the actual changed-path live verifier probe and retain redacted output.                                |
| Recovery path and rollback without weakening key history or breaking issued receipts                                               | Local rotation tests are supplemental only; no rehearsed production rollback/post-rollback compatibility record exists.                                                                                                                  | `MISSING`        | Rehearse rollback in the production release process, verify old/new receipts and historical keys before/after rollback, and record the immutable rollback target and probe results.                                                 |

## Final evidence packet (§10)

These rows are the twelve required packet items. They are intentionally not
counted as additional gates when their substance is already covered above.

| Item | Required final evidence                                                                 | Current authoritative evidence                                                                                                                                                                | Status           | Exact close action                                                                                                                                                             |
| ---- | --------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1    | Clean installs and full CI in every changed repository                                  | Standalone clean-copy install and local full suite pass; remote CI and PAR/website clean installs are absent.                                                                                 | `PARTIAL`        | Run `npm ci` and the complete required suites from clean checkouts at each integration commit; record remote check names and artifacts.                                        |
| 2    | Canonical coherent-active, coherent-inactive, contradictory, and indeterminate fixtures | Core, service, provider, CLI, MCP, and immutable fixture records cover all four outcome classes, with exact producer omit semantics and redacted reports.                                     | `PROVEN` (local) | Re-run the canonical fixture matrix after integration and retain one redacted report plus expected classification per outcome.                                                 |
| 3    | CLI human/JSON, exit codes, MCP discovery/calls/errors/cancellation/clean shutdown      | MCP lifecycle/parity is locally evidenced; CLI adapter tests pass, but final packaged CLI golden packet is not recorded.                                                                      | `PARTIAL`        | Capture process-level CLI golden stdout/stderr/exit cases from the committed package and pair them with raw MCP discovery/call/cancel/EOF results.                             |
| 4    | Inspector and applicable conformance results                                            | Pinned Inspector legacy/modern checks and applicable conformance scenarios pass locally; remote enforcement remains open.                                                                     | `PROVEN` (local) | Repeat once in the committed remote workflow and retain versions, scenario counts, exclusions, and check IDs.                                                                  |
| 5    | `npm pack` and empty-consumer execution                                                 | Package smoke passes with the current 125,355-byte tarball, SHA-256 `b6756af9845676eb9758b31a7c1992deb0be07eba8ae40483407251519f2b3ec`, 144 files, public import, trust loader, and bin help. | `PROVEN` (local) | Re-run from the committed Git checkout, compare the exact digest, and retain the empty-consumer transcript in the release packet.                                              |
| 6    | Bounded canonical live PAR verification with a non-sensitive asset ID                   | Only a WIP canonical JWKS/status compatibility observation is recorded; it does not prove asset-specific bundle verification.                                                                 | `PARTIAL`        | After PAR/website deployment and key freeze, run the real provider/service/CLI path on an approved non-sensitive test asset and record redacted classification/request counts. |
| 7    | Historical-key verification and rotation simulation                                     | Local two-key overlap, retirement, unknown-kid, and manifest-pin tests pass; deployed retention horizon is missing.                                                                           | `PARTIAL`        | Record PAR ring publication and run pre/post-rotation and post-rollback receipt probes against the deployed service.                                                           |
| 8    | `audit=omit` runtime and query evidence                                                 | Standalone schemas and fixture enforce omitted-audit placeholders; no production route/query non-invocation evidence exists.                                                                  | `MISSING`        | Run instrumented PAR `verification-bundle?audit=omit`, prove audit aggregation is not called, and record route/query/wire evidence at the PAR commit.                          |
| 9    | Website canonical registry origin and exact deployment revisions                        | Canonical origin is documented and DNS/TLS was observed; website configuration and immutable deployment revision are not recorded.                                                            | `MISSING`        | Verify `REGISTRY_URL=https://par.myproof.ai`, deploy exact website/PAR commits, and record immutable deployment IDs plus the changed-path probe.                               |
| 10   | Remote CI check names, event coverage, and enforcement classification                   | Workflow-contract config and local JSON are present; no remote check run/enforcement record is present.                                                                                       | `PARTIAL`        | Trigger all required events, record check names/IDs and enforced/advisory/unavailable/not-evaluated classification, and attach uploaded evidence references if configured.     |
| 11   | Rollback rehearsal and post-rollback compatibility                                      | No production rehearsal is recorded.                                                                                                                                                          | `MISSING`        | Execute the prepared rollback, verify issued old/new receipts and key history, and record the target revision plus post-rollback live result.                                  |
| 12   | Independent audit score and disposition for every implementation lane                   | MCP record's `99/100` is an owning-lane self-score only; no independent Sol score/disposition is recorded.                                                                                    | `MISSING`        | Have the independent Sol auditor review the committed implementation/evidence, require at least `97/100` with no blocker/high finding, and record every lane disposition.      |

## Overall disposition and score

The candidate has strong local proof for the foundation, contracts, core
cryptography, provider defenses, MCP adapter, Inspector/conformance paths,
security checks, package smoke, and the current `20 files / 296 tests` local
aggregate. Those results do not close the explicit PAR durability, website
deployment, remote CI enforcement, committed provenance, asset-specific live
verification, rollback, or independent-audit gates.

Current overall disposition: `PARTIAL`; release completion is not authorized.
The only numeric score currently recorded is the MCP lane's self-score of
`99/100`. It is not an independent audit score and cannot override a hard
gate. The contract-required independent score is `>=97/100` with no blocker or
high finding, and is currently `MISSING`.

The root integrator may change a row to `PROVEN` only after recording the exact
post-commit, remote, or deployment evidence named in that row. Until every
required row is closed and the independent audit passes, §15 stop conditions
remain active and §16 completion must remain `PARTIAL`.
